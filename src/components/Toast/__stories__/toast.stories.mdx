import { useState } from "react";
import Toast from "../Toast";
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { createComponentTemplate } from "../../../storybook/functions/create-component-story";
import Button from "../../Button/Button.jsx";
import { Link } from "../../../storybook/components";
import { TOOLTIP, ATTENTION_BOX, ALERT_BANNER } from "../../../storybook/components/related-components/component-description-map";
import "./toast.stories.scss";

<Meta
  title="Components/Toast"
  component={ Toast }
/>

<!--- Component template -->

export const toastTemplate = createComponentTemplate(Toast);

<!--- Component documentation -->

# Toast
- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)
- [Do’s and don’ts](#dos-and-donts)
- [Use cases and examples](#use-cases-and-examples)
- [Related components](#related-components)
- [Feedback](#feedback)

## Overview
A toast notification is a message object that presents timely information, including confirmation of actions, alerts, and errors.

<Canvas>
  <Story name="Overview"
         args={ {prop1: "prop's default value", prop2: "prop's default value"} }>
    { toastTemplate.bind({}) }
  </Story>
</Canvas>

## Props
<ArgsTable of={ Toast } />

## Usage
<UsageGuidelines guidelines={[
  "Use toast notifications immediately after a specific event such as a user action that does not relate to an object on the page. Toast used as a feedback loop to a user’s action.",
  "Toasts should appear one at a time, and only disappear when no longer required.",
  "Always be clear, concise and, where possible, give follow up actions to allow the user to become more informed or resolve the issue.",
  "Always provide an action button or option to undo.",
  "Toast should overlay permanent UI elements without blocking important actions.",
]}/>

<Tip title="Check yourself">Need to inform the user about a system’s action? Use an <Link href="/?path=/docs/components-alertbanner--overview">Alert banner</Link> instead.</Tip>

## Variants
### Default with button
<Canvas>
  <Story name="Default with button">
    {() => {
      const [toastOpen, setToastOpen] = useState(false);
      const [toastOpenButton, setToastOpenButton] = useState(false);
      const toggleToast = () => setToastOpen(open => !open);
      const closeToast = () => setToastOpen(false);
      return (
        <>
          <Button onClick={() => toggleToast()}>Toast with button</Button>
          <Toast
            open={toastOpen}
            onClose={() => closeToast()}
            actions={[{ type: Toast.actionTypes.BUTTON, content: "Button" }]}
          >
            General message toast
          </Toast>
        </>
      )
    }}
  </Story>
</Canvas>

### Toast with link
<Canvas>
  <Story name="Toast with link">
    {() => {
      const [toastOpen, setToastOpen] = useState(false);
      const [toastOpenButton, setToastOpenButton] = useState(false);
      const toggleToast = () => setToastOpen(open => !open);
      const closeToast = () => setToastOpen(false);
      return (
        <>
          <Button onClick={() => toggleToast()}>Toast with link</Button>
          <Toast
            open={toastOpen}
            onClose={() => closeToast()}
             actions={[{ type: Toast.actionTypes.LINK, text: "Link to action", href: "https://monday.com" }]}
          >
            General message toast
          </Toast>
        </>
      )
    }}
  </Story>
</Canvas>

### Success message
Use to providing a feedback loop. For example: Item copied.
<Canvas>
  <Story name="Success message">
    {() => {
      const [toastOpen, setToastOpen] = useState(false);
      const [toastOpenButton, setToastOpenButton] = useState(false);
      const toggleToast = () => setToastOpen(open => !open);
      const closeToast = () => setToastOpen(false);
      return (
        <>
          <Button onClick={() => toggleToast()}>Success message</Button>
          <Toast
            open={toastOpen}
            onClose={() => closeToast()}
            type={Toast.types.POSITIVE}
            actions={[{ type: Toast.actionTypes.BUTTON, content: "Undo 5" }]}
          >
           Positive message toast
          </Toast>
        </>
      )
    }}
  </Story>
</Canvas>

### Error message
Use when something was deleted, a problem has occured, etc.
<Canvas>
  <Story name="Error message">
    {() => {
      const [toastOpen, setToastOpen] = useState(false);
      const [toastOpenButton, setToastOpenButton] = useState(false);
      const toggleToast = () => setToastOpen(open => !open);
      const closeToast = () => setToastOpen(false);
      return (
        <>
          <Button onClick={() => toggleToast()}>Error message</Button>
          <Toast
            open={toastOpen}
            onClose={() => closeToast()}
            type={Toast.types.NEGATIVE}
            actions={[{ type: Toast.actionTypes.BUTTON, content: "Undo 5" }]}
          >
           Negative message toast
          </Toast>
        </>
      )
    }}
  </Story>
</Canvas>

## Do’s and Don’ts
<ComponentRules
  rules={[
    {
      positive: {
        component:<Toast />,
        description:"Best practice description."
      },
      negative: {
        component:<Toast />,
        description:"Bad practice description."
      }
    },
    {
      positive: {
        component:<Toast />,
        description:"Best practice description."
      },
      negative: {
        component:<Toast />,
        description:"Bad practice description."
      }
    },
    {
      positive: {
        component:<Toast />,
        description:"Best practice description."
      },
      negative: {
        component:<Toast />,
        description:"Bad practice description."
      }
    },
    {
      positive: {
        component:<Toast />,
        description:"Best practice description."
      },
      negative: {
        component:<Toast />,
        description:"Bad practice description."
      }
    }
  ]}
/>


## Use cases and examples

### Feedback loop
After a user has done an action, provide feedback to close the loop. For example, when an item has been deleted, duplicated, etc.
<Canvas>
  <Story name="Feedback loop">
    <Toast />
    <Toast />
  </Story>
</Canvas>

## Related components
<RelatedComponents componentsNames={[ALERT_BANNER,TOOLTIP,ATTENTION_BOX]} />