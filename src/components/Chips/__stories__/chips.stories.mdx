import { useState, useCallback } from "react";
import Chips from "../Chips";
import { createComponentTemplate } from "../../../storybook/functions/create-component-story";
import { ArgsTable, Story, Canvas, Meta } from "@storybook/addon-docs";
import { BUTTON_GROUP, SPLIT_BUTTON, LABEL } from "../../../storybook/components/related-components/component-description-map";
import "./chips.stories.scss";
import { MultipleStoryElementsWrapper } from "../../../storybook/components";
import { Calendar, Remove, Add, Email } from "../../Icon/Icons";

<Meta title="Components/Chips/Chip" component={Chips} />

<!--- Component template -->

export const chipsTemplate = createComponentTemplate(Chips);

<!--- Component documentation -->

# Chip

- [Overview](#overview)
- [Props](#props)
- [Usage](#usage)
- [Variants](#variants)
- [Do’s and don’ts](#dos-and-donts)
- [Anatomy](#anatomy)
- [Use cases and examples](#use-cases-and-examples)
- [Related components](#related-components)
- [Change log](#change-log)
- [Feedback](#feedback)

## Overview

Chips are compact elements that represent an input, attribute, or action. They may display text, icons, or both.

<Canvas>
  <Story name="Overview"
        args={{label: "This is a chip", readOnly: true}}>
    {chipsTemplate.bind({})}
  </Story>
</Canvas>

## Props
<ArgsTable of={Chips} />

## Usage
<UsageGuidelines guidelines={[
  "Chips represent a complex piece of information in compact form, such as an entity (person, place, or thing) or text. They enable user input and verify that input by converting text into chips.",
  "Use 8px spacing between chips.",
  "Use chips when content is mapped to multiple categories, and the user needs a way to differentiate between them."
]}/>


<Tip title="Tip">Chips will always show up in context of a field. If you just need to add information, use Label.</Tip>

## Variants
### With icons

<Canvas>
  <Story name="With icons">
    <Chips label="Chip with left icon" leftIcon={Email} readOnly /> 
    <Chips label="Chip with right icon" rightIcon={Email} readOnly /> 
  </Story>
</Canvas>

### Themes
<Canvas>
  <Story name="Themes">
    <Chips label="This is a long chip" /> 
    <Chips label="Chip negative" color={Chips.colors.NEGATIVE} /> 
    <Chips label="Chip positive" color={Chips.colors.POSITIVE} /> 
    <Chips label="Disabled" disabled={true} /> 
  </Story>
</Canvas>

## Do’s and Don’ts
<ComponentRules
  rules={[
    {
      positive: {
        component: <Chips label="Jason Tal" readOnly />,
        description:"When using a chip, the width will automatically size itself to fit the content."
      },
      negative: {
        component:<Chips label="This is a chip" readOnly />,
        description:"Don’t change the chip width."
      }
    }
  ]}
/>


## Use cases and examples
### Colorful chips for different content
Sometimes when needed, chips can change fill color. 


### Chips in Person Column
Chips can be removable.

## Related components
<RelatedComponents componentsNames={[LABEL, LABEL, BUTTON_GROUP]}/>